

const Ajuda = ( {back, logout} )=>{


    return(
        <div>
            <h1>Página de Ajuda!!!!!!!</h1>
            <button  onClick={back}>Home</button>
            <button onClick={logout} >Logout</button>
        </div>
    )
}

export default Ajuda